type Query {
  hello(name: String): String!
}

type Mutation {
  registrarse(
    email: String!
    password: String!
    nombre: String!
    apPaterno: String!
    apMaterno: String!
    celular: String!
  ): Boolean
}
