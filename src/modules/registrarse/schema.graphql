#import Error from "../../shared.graphql"

type Query {
  bye: String
}

type Mutation {
  registrarse(
    email: String!
    password: String!
    nombre: String!
    apPaterno: String!
    apMaterno: String!
    celular: String!
  ): [Error!]
}
