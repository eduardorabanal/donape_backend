#import Error from "../../shared.graphql"

type Query {
  bye: String
}

type Mutation {
  registrarse(
    email: String!
    password: String!
    nombre: String!
    celular: String!
  ): [Error!]
}
