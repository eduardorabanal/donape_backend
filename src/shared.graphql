type Error {
  path: String!
  message: String!
}
type UserResponse {
  success: Boolean!
  token: String
  errors: [Error!]
}
type Publicacion {
  id: ID!
  usuarioId: ID!
  titulo: String!
  descripcion: String!
  fecha: String!
}
